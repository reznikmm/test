on: [push]
jobs:
  hello:
    runs-on: ubuntu-latest
    name: A job to say hello
    steps:
    - name: Hello world action step
      id: hello
      uses: reznikmm/fedora-ada-action@v1
      with:
        spec: package/aaa.spec
